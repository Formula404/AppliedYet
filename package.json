{
  "name": "tou-le-ma",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm --prefix apps/desktop run dev",
    "build": "npm --prefix apps/desktop run build",
    "preview": "npm --prefix apps/desktop run preview",
    "typecheck": "npm --prefix apps/desktop run typecheck",
    "check:rust": "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --all -- --check && cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets --all-features -- -D warnings && cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml --all-features",
    "check": "npm run typecheck && npm run build && npm run check:rust",
    "tauri": "npm --prefix apps/desktop run tauri",
    "tauri:build": "npm --prefix apps/desktop run tauri:build",
    "tauri:build:signed": "npm --prefix apps/desktop run tauri:build:signed"
  }
}
